# DINO ResNet-50 实验总结

## 1. 项目背景

### 1.1 模型信息
- **架构**: ResNet-50
- **训练方法**: DINO（Self-DIstillation with NO labels）
- **训练参数**: global crop size=512, local crop size=256, 218 epoch
- **Checkpoint 路径**: `dino_output/resnet50/checkpoint.pth`
- **对比模型**: Meta 官方预训练 DINO ResNet-50（ImageNet 1.2M 图片，300 epoch，224 分辨率）

### 1.2 数据集
- **路径**: `/data2/zhn/code/data/jinxiang/`
- **类别**: 4 类（JLD、JZ、TT、ZZ），为金相图像的不同业务类型
- **类别分布**:
  - JLD: 1618 张
  - JZ: 3159 张
  - TT: 1876 张
  - ZZ: 1023 张
  - **总计: 7676 张**
- **特点**: 同类内图像差异较小（同类金相图片高度相似）

### 1.3 运行环境
- Docker 容器: JHCVTrain
- Conda 虚拟环境: ai
- GPU: 8GB 显存

---

## 2. 数据集划分

### 2.1 划分方案
- 按 8:2 比例随机划分，固定 seed=42
- 使用软链接组织，不复制文件，不污染原始数据
- 划分结果存放在 `/data2/zhn/code/data/jinxiang_split/`

### 2.2 划分结果

| 类别 | 训练集 | 验证集 |
|------|--------|--------|
| JLD  | 1294   | 324    |
| JZ   | 2527   | 632    |
| TT   | 1500   | 376    |
| ZZ   | 818    | 205    |
| **总计** | **6139** | **1537** |

---

## 3. k-NN 评估

### 3.1 评估原理
k-NN 是零参数分类器，不训练任何参数，纯粹通过特征空间中的最近邻投票来分类。能直接反映模型特征空间中同类聚集、异类分离的程度。

### 3.2 初始评估（train=val，同一份数据）

最初未划分数据集，train 和 val 都指向同一份数据（7676 张），k-NN 做的是"自己找自己"：

| k 值 | 自训练模型 | 预训练模型 |
|------|-----------|-----------|
| 10   | 98.44%    | 100%      |
| 20   | 97.71%    | 100%      |
| 50   | 95.31%    | 100%      |
| 100  | 93.55%    | 100%      |

**结论**: 预训练模型接近完美，自训练模型也很高但存在差距。但由于 train=val，结果有水分。

### 3.3 独立 train/val 划分后评估（224 分辨率）

使用 8:2 划分后的独立数据集，但评估时使用了默认的 224 分辨率输入：

| k 值 | 自训练模型 | 预训练模型 |
|------|-----------|-----------|
| 10   | 98.24%    | 100%      |
| 20   | 97.66%    | 100%      |
| 50   | 94.93%    | 100%      |
| 100  | 92.97%    | 100%      |

### 3.4 分辨率问题发现

训练时 global crop size=512，但评估脚本默认 Resize(256)→CenterCrop(224)，输入分辨率为 224，与训练分辨率严重不匹配。自训练模型在 512 分辨率上学到的特征细节在 224 评估时丢失。

### 3.5 修正分辨率后评估（512 分辨率）

将评估输入改为 Resize(576)→CenterCrop(512)，匹配训练分辨率：

| k 值 | 224 分辨率 | 512 分辨率（正确） | 提升 |
|------|-----------|-------------------|------|
| 10   | 97.85%    | **99.74%**        | +1.89% |
| 20   | 96.42%    | **99.22%**        | +2.80% |
| 50   | 94.99%    | **97.79%**        | +2.80% |
| 100  | 92.58%    | **96.16%**        | +3.58% |

**结论**: 分辨率匹配后自训练模型大幅提升，k=10 达到 99.74%，与预训练模型（100%）差距从 ~2% 缩小到 ~0.26%。

### 3.6 不同训练阶段 checkpoint 对比（512 分辨率）

| k 值 | epoch 0 | epoch 10 | epoch 217（最终） |
|------|---------|----------|-------------------|
| 10   | 93.36%  | 96.68%   | 98.24%（224）/ 99.74%（512） |
| 20   | 92.58%  | 95.77%   | 97.66%（224）/ 99.22%（512） |
| 50   | 89.59%  | 94.01%   | 94.93%（224）/ 97.79%（512） |
| 100  | 86.60%  | 92.26%   | 92.97%（224）/ 96.16%（512） |

**观察**: epoch 0（未训练）就有 93%，说明 4 分类任务偏简单，ResNet-50 随机初始化特征已有一定区分力。DINO 训练前期收敛快（10 epoch 提升约 3%），后期主要精细化局部特征。

### 3.7 代码修改
- 为评估脚本新增 `--input_size` 参数（默认 224），Resize 和 CenterCrop 自动按比例适配
- Resize 大小 = input_size × 256 / 224

---

## 4. 线性探测评估

### 4.1 评估原理
线性探测在冻结 backbone 上训练一个线性分类器（全连接层），通过梯度优化找最优分类边界。比 k-NN 更敏感，尤其对少类别任务区分度更高。

### 4.2 自训练模型（224 分辨率，batch_size=128）

| Epoch | 训练 Loss | 验证 Loss | 验证 Acc@1 |
|-------|----------|----------|------------|
| 0     | 0.554    | 0.389    | 82.7%      |
| 3     | 0.334    | 0.313    | 86.1%      |
| 5     | 0.311    | 0.295    | 86.3%      |
| 10    | 0.270    | 0.274    | 87.2%      |
| 14    | 0.254    | 0.254    | 87.9%      |

### 4.3 预训练模型（224 分辨率，batch_size=128）

| Epoch | 训练 Loss | 验证 Loss | 验证 Acc@1 |
|-------|----------|----------|------------|
| 0     | 1.239    | 1.095    | 80.8%      |
| 3     | 0.632    | 0.599    | 96.6%      |
| 5     | 0.469    | 0.450    | 98.4%      |
| 10    | 0.286    | 0.276    | 99.2%      |
| 14    | 0.223    | 0.213    | 99.5%      |

**观察**: 预训练模型收敛极快（epoch 3 即 96.6%），自训练模型在 224 分辨率下仅 87.9%，差距约 12%。

### 4.4 自训练模型（512 分辨率，batch_size=32）

| Epoch | 训练 Loss | 验证 Loss | 验证 Acc@1 |
|-------|----------|----------|------------|
| 0     | 0.507    | 0.412    | 83.5%      |
| 3     | 0.324    | 0.293    | 92.7%      |
| 5     | 0.293    | 0.261    | 94.6%      |
| 10    | 0.249    | 0.223    | 95.3%      |
| 14    | 0.232    | 0.195    | 95.3%      |
| 19    | 0.213    | 0.177    | 95.5%      |

**对比总结（epoch 14）**:

| 模型 | 分辨率 | Acc@1 |
|------|--------|-------|
| 自训练 DINO | 224 | 87.9% |
| 自训练 DINO | 512 | **95.3%** |
| 预训练 DINO | 224 | 99.5% |

**结论**: 分辨率匹配后自训练模型从 87.9% 提升到 95.3%，提升约 8%。但与预训练模型仍有约 4% 差距，主要因为训练数据量差距（7.6K vs 120 万）。

---

## 5. 语义分割下游任务验证

### 5.1 任务说明
以 JLD（晶粒度）数据集为下游任务，验证 DINO 特征在语义分割上的迁移效果。

### 5.2 数据集
- **图片**: 82 张 RGB 图片（2448×2048），存放在 `JLD/img/`
- **标签**: 82 张灰度 PNG mask（2448×2048），存放在 `JLD/label/`
- **类别**: 3 类（像素值 0=背景, 1=类别1, 2=类别2）
- **数据特点**: 大部分图片只有类别 0 和 2，约 20 张图片包含完整的 0/1/2 三类
- 按 8:2 划分：训练集 65 张，验证集 17 张

### 5.3 模型设计
- **Backbone**: ResNet-50（冻结），加载 DINO 权重
- **特征提取**: 提取 layer4 输出（2048 通道，32 倍下采样）
- **分割头**: Conv2d(2048→256) → BatchNorm → ReLU → Conv2d(256→3) → 双线性插值回原图大小
- **训练**: CrossEntropyLoss，SGD 优化器，只训练分割头参数
- **评估指标**: mIoU（mean Intersection over Union）

### 5.4 第一组数据结果（55 张图，标签只有 0 和 2）

三组结果几乎无差别，mIoU 约 34%，类别 1 IoU 始终为 0。原因是标签数据存在问题（大部分图缺少类别 1）。

### 5.5 第二组数据结果（82 张图，标签 0/1/2）

| 模型 | 分辨率 | batch_size | mIoU | 背景 IoU | cls1 IoU | cls2 IoU |
|------|--------|-----------|------|---------|---------|---------|
| 自训练 DINO | 512 | 2 | **59.6%** | 84.3% | 44.6% | 49.8% |
| 自训练 DINO | 512 | 4 | **54.2%** | 83.6% | 30.8% | 48.1% |
| 预训练 DINO | 224 | 2 | **73.4%** | 93.0% | 47.4% | 79.6% |
| 随机初始化 | 512 | 2 | **70.0%** | 89.8% | 49.3% | 71.0% |

### 5.6 分割实验分析

**预训练模型表现最好**（73.4%），但随机初始化也达到了 70%，说明：
1. 分割头过于简单（仅用 layer4 单层特征，32 倍下采样），空间信息丢失严重，无法充分体现 backbone 特征质量差异
2. 冻结 backbone 的设置天然有利于泛化性更强的预训练模型
3. 自训练模型在简单 FCN head + 冻结 backbone 设置下表现不佳，不能简单否定其价值
4. 类别 1 是最难分的类别，三组 IoU 都在 30-49% 之间

**后续优化方向**:
- 使用多尺度特征融合（FPN），结合 layer2/layer3/layer4 的特征
- 全量微调（backbone 参与训练），而非冻结
- 使用更成熟的分割框架（DeepLabV3 / UPerNet）

---

## 6. 总体结论

### 6.1 自训练模型的优势
- k-NN 在正确分辨率（512）下达到 99.74%，非常接近预训练模型
- 线性探测 95.3%，仅用 7.6K 张图达到较高水平
- Grad-CAM 特征可视化效果好（用户主观评价）

### 6.2 自训练模型的不足
- 训练数据量有限（7.6K vs 120 万），特征泛化能力不如预训练模型
- 在冻结 backbone + 简单分割头的设置下，分割效果不如预训练模型和随机初始化
- 需要更完善的下游任务评估框架来公平比较

### 6.3 关键发现
- **评估分辨率必须与训练分辨率匹配**，否则结果严重失真
- **4 分类任务的 k-NN 区分度有限**，即使未训练的模型也有 93%
- **线性探测比 k-NN 更敏感**，能更好地区分特征质量
- **下游任务评估需要合适的框架**，简单的 FCN head 无法充分评估特征质量
